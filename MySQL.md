好的，以下是基于你提供的 **MySQL 面试题** 的**满分参考答案**。我对你的原始答案进行了补充、纠错和深度扩展，整理成一份可以直接用于面试准备的完整文档。

---

# MySQL 面试题汇总

## 一、存储引擎

### 1.1 MySQL 数据库引擎有哪些？

**满分答案：**

MySQL 常用的存储引擎主要有：

| 存储引擎        | 说明                                                 | 适用场景                                       |
| --------------- | ---------------------------------------------------- | ---------------------------------------------- |
| **InnoDB**      | MySQL 5.5 后的默认引擎，支持事务、行级锁、外键、MVCC | 绝大多数 OLTP 场景（如订单、用户、支付）       |
| **MyISAM**      | 早期默认引擎，不支持事务，表级锁，查询速度快         | 只读或读多写少的场景（如日志、报表、数据仓库） |
| **Memory**      | 基于内存，速度极快，重启后数据丢失                   | 临时表、缓存、会话数据                         |
| **CSV**         | 以 CSV 格式存储，可对文件直接操作                    | 数据导入导出中转                               |
| **Archive**     | 只支持 INSERT 和 SELECT，压缩存储                    | 日志归档、历史数据存储                         |
| **NDB Cluster** | 分布式集群引擎，支持高可用                           | 高可用、高吞吐的分布式场景                     |

> 面试重点：**InnoDB 和 MyISAM 的区别**（见下题）。

---

### 1.2 InnoDB 和 MyISAM 的区别？

**满分答案：**

| 对比项       | InnoDB                             | MyISAM                       |
| ------------ | ---------------------------------- | ---------------------------- |
| **事务**     | ✅ 支持                             | ❌ 不支持                     |
| **锁粒度**   | 行级锁（并发写入好），也支持表锁   | 表级锁（写入会阻塞其他读写） |
| **外键**     | ✅ 支持                             | ❌ 不支持                     |
| **MVCC**     | ✅ 支持（多版本并发控制）           | ❌ 不支持                     |
| **全文索引** | MySQL 5.6 后支持                   | ✅ 支持（原生）               |
| **聚簇索引** | ✅ 使用聚簇索引，数据和索引一起存储 | ❌ 非聚簇索引，数据和索引分离 |
| **崩溃恢复** | ✅ 通过 redo log 自动恢复           | ❌ 崩溃后可能损坏，需修复     |
| **缓存**     | 缓存数据和索引（buffer pool）      | 只缓存索引（key buffer）     |
| **适用场景** | OLTP（高并发、事务、强一致性）     | 只读、读多写少、数据仓库     |

> **面试官追问**：为什么 InnoDB 默认使用行锁而 MyISAM 使用表锁？——InnoDB 支持事务和 MVCC，需要通过行锁实现更高的并发；MyISAM 不支持事务，设计上追求简单和查询速度，表锁实现简单且避免死锁。

---

## 二、事务与隔离级别

### 2.1 什么是事务？

**满分答案：**

事务是数据库执行的一组操作单元，具有 **ACID** 特性：

| 特性                      | 含义                                   | 实现方式                           |
| ------------------------- | -------------------------------------- | ---------------------------------- |
| **原子性（Atomicity）**   | 事务中的操作要么全部成功，要么全部失败 | undo log（回滚日志）               |
| **一致性（Consistency）** | 事务前后，数据完整性约束没有被破坏     | 通过原子性、隔离性、持久性共同保证 |
| **隔离性（Isolation）**   | 多个事务并发执行时，互不干扰           | 锁机制 + MVCC                      |
| **持久性（Durability）**  | 事务提交后，数据永久保存               | redo log（重做日志）               |

> **示例**：转账 A 扣钱、B 加钱，要么都完成，要么都不发生。

---

### 2.2 并发事务带来的问题？

**满分答案：**

| 问题                                  | 定义                                                         | 示例                                                         |
| ------------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **脏读（Dirty Read）**                | 一个事务读取了另一个未提交事务修改的数据                     | 事务A修改余额100→200未提交，事务B读取到200，然后A回滚，B读到的200就是脏数据 |
| **不可重复读（Non-repeatable Read）** | 同一事务内两次读取同一条记录，结果不一致（由于其他事务更新并提交） | 事务A读取余额100，事务B修改为200并提交，事务A再次读取发现变成200 |
| **幻读（Phantom Read）**              | 同一事务内两次查询记录数不一致（由于其他事务插入或删除）     | 事务A查询用户数得10，事务B插入一条新用户并提交，事务A再次查询得到11 |

> **区别**：不可重复读侧重于**修改/删除**，幻读侧重于**插入/删除导致数量变化**。MySQL 的 REPEATABLE-READ 级别通过间隙锁（Gap Lock）一定程度上解决了幻读。

---

### 2.3 事务的隔离级别？

**满分答案：**

| 隔离级别                         | 脏读   | 不可重复读 | 幻读   | 说明                                             |
| -------------------------------- | ------ | ---------- | ------ | ------------------------------------------------ |
| **READ UNCOMMITTED**（读未提交） | ✅ 可能 | ✅ 可能     | ✅ 可能 | 最低隔离，性能最好，安全性最差                   |
| **READ COMMITTED**（读已提交）   | ❌      | ✅ 可能     | ✅ 可能 | Oracle 默认，避免脏读                            |
| **REPEATABLE READ**（可重复读）  | ❌      | ❌          | ✅ 可能 | **MySQL 默认**，通过 MVCC + 间隙锁避免大部分幻读 |
| **SERIALIZABLE**（串行化）       | ❌      | ❌          | ❌      | 最高隔离，所有事务串行执行，性能最差             |

> **面试官追问**：MySQL 的 REPEATABLE READ 能完全解决幻读吗？——InnoDB 通过**间隙锁（Gap Lock）** 在范围查询时锁定间隙，防止其他事务插入，因此可以**避免标准定义的幻读**，但在某些极端场景（如快照读 vs 当前读）仍可能出现逻辑不一致，需要根据业务选择。

---

### 2.4 redo log 和 binlog 的区别？为什么需要两阶段提交？

这是事务和日志的高频追问。

| 对比维度     | redo log（重做日志）                       | binlog（归档日志）                 |
| ------------ | ------------------------------------------ | ---------------------------------- |
| **所属层**   | InnoDB 存储引擎层                          | MySQL Server 层                    |
| **记录内容** | 物理日志："在某个数据页上做了什么修改"     | 逻辑日志：SQL 语句或行数据变更     |
| **写入方式** | 循环写（固定大小，写满后覆盖）             | 追加写（文件写满后切换新文件）     |
| **主要用途** | **崩溃恢复（crash-safe）**，保证持久性     | 主从复制、数据恢复、数据同步       |
| **刷盘时机** | 事务提交前分阶段写入（`innodb_flush_log_at_trx_commit`） | 事务提交时写入（`sync_binlog`） |

**为什么需要两阶段提交？**

redo log 和 binlog 是两个独立系统，如果不协调，会导致主从数据不一致：
- **先写 redo log 后写 binlog**：主库崩溃恢复后数据已更新，但从库没有对应的 binlog，导致从库丢失该更新
- **先写 binlog 后写 redo log**：主库崩溃后 redo log 丢失，数据回滚，但从库已通过 binlog 应用了该更新，导致从库多了数据

**两阶段提交流程**：
1. **Prepare 阶段**：写入 redo log，标记为 `prepare` 状态
2. **写入 binlog**：将事务产生的 binlog 写入磁盘
3. **Commit 阶段**：将 redo log 标记为 `commit` 状态

崩溃恢复时：如果 redo log 处于 prepare 状态，则检查 binlog 是否完整——完整则提交，不完整则回滚。这样保证了 redo log 和 binlog 的一致性。

---

## 三、索引

### 3.1 什么是索引？

**满分答案：**

索引是一种**用于快速查询数据的数据结构**，类似于书的目录。MySQL 中索引在存储引擎层实现，InnoDB 使用 **B+ 树** 作为默认索引结构。

**索引的作用：**
- 加快数据检索速度
- 保证数据的唯一性（唯一索引）
- 加速表连接（JOIN）
- 减少排序和分组的时间

---

### 3.2 MySQL 为什么使用 B+ 树？（高频追问）

这是索引部分的核心追问，"为什么用 B+ 树而不是 B 树/二叉树/红黑树/Hash"需要逐层递进回答。

**① 为什么不用二叉树/红黑树？**
- 二叉树在极端情况下退化为链表（如顺序插入 1,2,3,4...），查询从 O(log N) 退化为 O(N)
- 红黑树虽然自平衡，但树高度随数据量快速增长——1000万条数据，树高约 24 层，每次查询需要 ~24 次磁盘 I/O，不可接受

**② 为什么不用 B 树？**
- B 树非叶子节点也存储数据，导致每个节点能存储的索引 key 更少，树更高，I/O 次数更多
- B+ 树非叶子节点只存 key + 指针，不存数据，单个节点能存更多 key → 树更矮 → I/O 更少
- 典型对比：同样 3 层，B+ 树可存 ~2000万 行（InnoDB 默认 16KB 页），B 树可能只有百万级

**③ 为什么不用 Hash 索引？**
- Hash 只适合等值查询（`=`、`IN`），不支持范围查询（`>`, `<`, `BETWEEN`）
- Hash 不支持排序和分组（`ORDER BY`、`GROUP BY`）
- Hash 存在冲突问题，冲突多时需链地址法解决，效率下降
- InnoDB 支持自适应 Hash 索引（Adaptive Hash Index），对频繁等值查询的热点页自动建立 Hash 索引，作为 B+ 树的补充

**④ B+ 树的额外优势**：
- **叶子节点构成有序双向链表**，范围查询只需找到起点然后遍历链表，非常高效
- **查询稳定性**：所有查询都要走到叶子节点（不像 B 树可能在非叶子节点命中），查询路径长度一致

| 数据结构 | 查找复杂度 | 范围查询     | 适合磁盘场景  | MySQL 使用         |
| -------- | ---------- | ------------ | ------------- | ------------------ |
| 二叉树   | O(log N)   | ✅（中序遍历）| ❌ 树太高       | ❌                  |
| 红黑树   | O(log N)   | ✅            | ❌ 树仍较高     | ❌                  |
| B 树     | O(log N)   | ❌ 需要回退   | ✅ 每层 key 较少 | ❌                  |
| B+ 树    | O(log N)   | ✅ 叶子链表   | ✅ 最优          | ✅ **InnoDB 默认**  |
| Hash     | O(1)       | ❌ 不支持     | ❌              | ✅ 自适应 Hash 补充 |

> **一句话总结**：B+ 树是专为磁盘 I/O 设计的结构——矮、胖、叶子有序——用最少的 I/O 次数支持等值+范围查询。

---

### 3.3 索引的优缺点？

**满分答案：**

| 优点                   | 缺点                                         |
| ---------------------- | -------------------------------------------- |
| 大幅提高查询速度       | 索引本身占用磁盘空间                         |
| 加速排序和分组         | 增、删、改操作需要同时维护索引，降低写入性能 |
| 唯一索引保证数据唯一性 | 创建和维护索引需要时间                       |

> 原则：**合理创建索引，避免冗余索引**。

---

### 3.4 MySQL 索引有哪些类型？

**满分答案：**

| 索引类型                        | 说明                                       | 特点                            |
| ------------------------------- | ------------------------------------------ | ------------------------------- |
| **主键索引（PRIMARY KEY）**     | 唯一且非空，每张表只能有一个               | 聚簇索引，数据按主键顺序存储    |
| **唯一索引（UNIQUE）**          | 列值必须唯一，允许 NULL（但只有一个 NULL） | 加速查询 + 唯一约束             |
| **普通索引（INDEX）**           | 无唯一性要求                               | 加速查询                        |
| **全文索引（FULLTEXT）**        | 对文本内容进行分词搜索                     | 适用于 `MATCH AGAINST` 查询     |
| **组合索引（Composite Index）** | 多个列联合创建的索引                       | 遵循最左前缀原则                |
| **空间索引（SPATIAL）**         | 用于地理数据（GIS）                        | 仅 MyISAM 和 InnoDB（5.7+）支持 |

> 纠正：你写的"外键索引"不是索引类型，外键是约束，会自动创建索引；"非空索引"不存在，非空是约束。

---

### 3.5 什么时候不建议添加索引？

**满分答案：**

| 场景                             | 原因                 |
| -------------------------------- | -------------------- |
| 表数据量很小                     | 全表扫描更快         |
| 频繁写入的表（如日志、流水）     | 维护索引开销大       |
| 列中重复值很高（如性别）         | 索引选择性差，收益低 |
| 很少作为查询条件的列             | 浪费空间和写入性能   |
| 长文本或 BLOB 列（除非前缀索引） | 索引占用太大         |

---

### 3.6 使用索引需要注意什么？（索引失效场景）

**满分答案：**

| 失效场景                               | 示例                                                         |
| -------------------------------------- | ------------------------------------------------------------ |
| **违反最左前缀原则**                   | 组合索引 `(a,b,c)`，查询条件只有 `b` 或 `c`                  |
| **对索引列使用函数或计算**             | `WHERE YEAR(date) = 2025` → 应改为 `WHERE date BETWEEN '2025-01-01' AND '2025-12-31'` |
| **隐式类型转换**                       | `WHERE phone = 123456`（phone 是 varchar）→ 应传字符串       |
| **使用 `!=` 或 `<>`**                  | 通常会导致全表扫描，但索引选择性高时优化器仍可能走 range 扫描 |
| **使用 `LIKE '%xxx'`**（以通配符开头） | `LIKE '%abc'` 无法用索引                                     |
| **使用 `OR` 且两侧列不全有索引**       | 可能走全表                                                   |
| **数据分布不均**                       | 优化器认为全表扫描更快（如查询大部分数据）                   |

---

### 3.7 什么是聚簇索引和非聚簇索引？

**满分答案：**

| 类型                              | 说明                                               | InnoDB 示例                                                  |
| --------------------------------- | -------------------------------------------------- | ------------------------------------------------------------ |
| **聚簇索引（Clustered Index）**   | 数据行和索引存储在一起，叶子节点直接存放完整行数据 | 主键索引就是聚簇索引。如果没有主键，则选用第一个唯一非空索引；否则隐式生成一个 row_id |
| **非聚簇索引（Secondary Index）** | 索引和数据分开存储，叶子节点存放主键值，需要回表   | 普通索引、唯一索引都是非聚簇索引                             |

**回表（Back to Table）**：通过非聚簇索引查到主键值，再根据主键回聚簇索引查完整行数据。

**覆盖索引（Covering Index）**：当查询的字段全部在非聚簇索引中时，直接返回索引值，无需回表，性能很高。

---

### 3.8 什么是索引下推（ICP，Index Condition Pushdown）？

**满分答案：**

索引下推是 MySQL 5.6 引入的优化：将 WHERE 条件中**与索引相关的过滤条件**下推到存储引擎层过滤，减少回表和返回给 Server 层的数据量。

**举例**：组合索引 `(name, age)`，查询 `WHERE name LIKE '张%' AND age = 25`

- **没有 ICP**：存储引擎通过索引查出所有 `name LIKE '张%'` 的主键，回表取出完整行返回 Server 层，Server 层再过滤 `age = 25`
- **有 ICP**：存储引擎在索引层面直接判断 `age = 25`，只将满足条件的记录回表并返回

**效果**：大幅减少回表次数。

> ICP 默认开启（`optimizer_switch` 中的 `index_condition_pushdown=on`）。它不会改变执行计划的 type，但会在 Extra 中显示 `Using index condition`。

---

### 3.9 `int(11)` 中的 11 是什么意思？

**满分答案：**

`int(11)` 中的 **11 是显示宽度（display width）**，不影响存储范围，不影响实际数值，只在使用 `ZEROFILL` 时控制显示时填充多少个零。

- 存储范围：`int` 永远是 4 字节，范围 -2^31 到 2^31-1（约 -21亿 到 21亿）
- 显示宽度：例如 `int(5) ZEROFILL` 存储 123 会显示为 `00123`
- 不加 `ZEROFILL` 时，显示宽度无任何作用

> 纠正：很多新人误以为 11 表示最大长度，这是错误的。

---

## 四、SQL 优化

### 4.1 SQL 优化的手段有哪些？

**满分答案：**

| 优化方向                                    | 具体措施                                                     |
| ------------------------------------------- | ------------------------------------------------------------ |
| **避免 SELECT ***                           | 只查询需要的列，减少网络传输和回表                           |
| **使用 EXISTS 替代 IN**（子表数据量大时） | MySQL 8.0 优化器已能对 `IN` 子查询做 Semi-Join 半连接优化，两者执行计划往往相同。建议用 `EXPLAIN` 实际验证而非无脑替换 |
| **避免在 WHERE 中使用函数或计算**           | 如 `WHERE LEFT(name,3) = 'abc'` → 应改为 `WHERE name LIKE 'abc%'` |
| **避免 `!=` 或 `<>`**                       | 尽量使用范围查询或等值                                       |
| **优化 JOIN**                               | 小表驱动大表，确保关联字段有索引                             |
| **使用 LIMIT 限制返回行数**                 | 分页或只取 Top N                                             |
| **合理使用索引**                            | 根据查询条件创建必要索引                                     |
| **避免隐式类型转换**                        | 传入参数类型与字段类型一致                                   |
| **批处理操作**                              | 批量插入/更新，减少 IO 次数                                  |
| **分析慢查询日志**                          | 用 `EXPLAIN` 查看执行计划                                    |

---

### 4.2 `COUNT(*)` 在 InnoDB 中为什么慢？

**满分答案：**

InnoDB 没有像 MyISAM 那样维护一个全局的行数计数器，因为 InnoDB 支持 MVCC，不同事务看到的数据行数可能不同。

- `COUNT(*)` 需要逐行扫描数据或辅助索引来统计
- 优化方法：
  - 使用二级索引（比聚簇索引小，扫描更快）
  - 使用近似值：`SHOW TABLE STATUS` 中的 `Rows` 字段（不精确）
  - 单独维护计数表（如 Redis 缓存）

---

### 4.3 MySQL 中一条 SQL 如何执行？

**满分答案：**

执行流程（以查询为例）：

```
客户端 → 连接器（建立连接、权限验证） → 查询缓存（8.0 已移除） → 解析器（词法/语法分析，生成解析树） → 预处理器（检查表/列是否存在） → 优化器（选择索引，生成执行计划） → 执行器（调用存储引擎接口，返回结果）
```

**关键组件：**
- **连接器**：管理连接和权限
- **解析器**：SQL 语法校验
- **优化器**：决定使用哪个索引、表连接顺序
- **执行器**：与存储引擎交互，逐行处理

---

### 4.4 EXPLAIN 执行计划详解

这是 SQL 优化的核心工具，面试必问。

| 字段          | 含义                                                         | 重点关注值                                                   |
| ------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **`id`**      | SELECT 的执行顺序，id 越大越先执行                           | 嵌套子查询时有多个 id                                        |
| **`select_type`** | 查询类型：SIMPLE（简单查询）、PRIMARY（主查询）、SUBQUERY（子查询）、DERIVED（派生表） | 出现 DERIVED 需要关注                                         |
| **`type`**    | **访问方式（最关键）**，性能从好到差：                        | `system > const > eq_ref > ref > range > index > ALL`         |
|               | `const`：主键/唯一索引等值查询；`eq_ref`：关联查询用主键匹配；`ref`：普通索引等值匹配；`range`：索引范围扫描；`index`：全索引扫描；`ALL`：全表扫描 | ⚠️ `ALL` 和 `index` 需要优化                                  |
| **`possible_keys`** | 可能用到的索引                                               | 如果为 NULL 说明没有可用索引                                  |
| **`key`**      | 实际使用的索引                                               | 为 NULL 时说明没有用索引                                      |
| **`key_len`**  | 使用的索引字节数                                             | 越短越好，可判断是否使用了组合索引的全部列                    |
| **`rows`**     | 预估扫描行数                                                 | 越小越好，与实际行数差距大说明索引失效或统计信息不准          |
| **`Extra`**    | 额外信息                                                     | `Using index`（覆盖索引，最好）；`Using where`（Server层过滤）；`Using filesort`（额外排序，需优化）；`Using temporary`（临时表，需优化）；`Using index condition`（索引下推） |

> **面试技巧**：能说出 `type` 的性能排序和 `Extra` 中 `Using filesort`/`Using temporary` 的含义，基本就能过关。

---

### 4.5 深分页如何优化？

**满分答案：**

当 `LIMIT offset, size` 中 offset 非常大时（如 `LIMIT 1000000, 20`），MySQL 需要先扫描 1000020 行再丢弃前 1000000 行，极慢。

**优化方案：**

| 方案                       | 实现                                                         | 适用场景                           |
| -------------------------- | ------------------------------------------------------------ | ---------------------------------- |
| **延迟关联**               | `SELECT a.* FROM A a INNER JOIN (SELECT id FROM A ORDER BY id LIMIT 1000000,20) t ON a.id = t.id` | 有覆盖索引，先通过索引查主键再关联 |
| **基于上次游标（推荐）**   | `SELECT * FROM A WHERE id > 1000000 ORDER BY id LIMIT 20` | 有自增ID，翻页不需要随机跳页       |
| **应用层限制**             | 限制最多翻页数（如只允许前 100 页），或改用搜索而非翻页     | 搜索引擎场景                       |

> 生产实践中，**游标翻页**（基于上次 ID）是最常用的方案，微信朋友圈、微博 feed 流都采用这种方式。

---

## 五、表结构设计

### 5.1 `varchar` 和 `char` 的区别？

**满分答案：**

| 对比项   | CHAR                                 | VARCHAR                          |
| -------- | ------------------------------------ | -------------------------------- |
| 长度     | 固定长度（0-255）                    | 可变长度（0-65535）              |
| 存储空间 | 固定分配声明长度，不足时用空格填充   | 实际长度 + 1/2 字节记录长度      |
| 检索效率 | 稍快（固定偏移）                     | 稍慢（需计算实际长度）           |
| 适用场景 | 长度固定（如身份证号、MD5 值、性别） | 长度变化大（如地址、描述、JSON） |

> `varchar(30)` 表示**最大长度 30 个字符**（MySQL 4.0 以前是字节，之后是字符，取决于字符集）。

---

### 5.2 `drop`、`delete`、`truncate` 的区别？

**满分答案：**

| 操作       | 类型                | 是否可回滚 | 释放空间                 | 重置自增 | 触发器   | 速度 |
| ---------- | ------------------- | ---------- | ------------------------ | -------- | -------- | ---- |
| `DELETE`   | DML（数据操作语言） | ✅ 可回滚   | ❌ 不释放（只是标记删除） | ❌        | ✅ 触发   | 慢   |
| `TRUNCATE` | DDL（数据定义语言） | ❌ 不可回滚 | ✅ 立即释放               | ✅ 重置   | ❌ 不触发 | 快   |
| `DROP`     | DDL                 | ❌ 不可回滚 | ✅ 释放整个表             | —        | —        | 最快 |

**使用建议：**
- 删除少量数据：`DELETE` + `WHERE`
- 清空全表但要保留结构：`TRUNCATE`
- 删除整张表：`DROP`

---

### 5.3 什么是视图？

**满分答案：**

视图是一张**虚拟表**，其内容由 SQL 查询定义。不存储实际数据，查询时动态生成。

**优点：**
- 简化复杂查询（将多表关联封装成视图）
- 数据安全（隐藏敏感列）
- 逻辑独立性

**缺点：**
- 对视图的更新有限制（某些复杂视图不可更新）
- 性能不如直接查表（每次查询都要执行视图定义）

---

### 5.4 内连接、左外连接、右外连接的区分？

**满分答案：**

| 连接类型 | 关键字            | 返回结果                                                 |
| -------- | ----------------- | -------------------------------------------------------- |
| 内连接   | `INNER JOIN`      | 两表匹配的行，不匹配的丢弃                               |
| 左外连接 | `LEFT JOIN`       | 左表所有行 + 右表匹配的行（不匹配则右表字段为 NULL）     |
| 右外连接 | `RIGHT JOIN`      | 右表所有行 + 左表匹配的行（不匹配则左表字段为 NULL）     |
| 全外连接 | `FULL OUTER JOIN` | MySQL 不直接支持，可用 `LEFT JOIN UNION RIGHT JOIN` 模拟 |

**示例：**
```sql
-- 内连接：只返回有订单的用户
SELECT * FROM users INNER JOIN orders ON users.id = orders.user_id;

-- 左连接：返回所有用户，包括无订单的用户
SELECT * FROM users LEFT JOIN orders ON users.id = orders.user_id;
```

---

### 5.5 `utf8` 和 `utf8mb4` 的区别？（经典坑）

**满分答案：**

| 对比项   | utf8（MySQL）           | utf8mb4                   |
| -------- | ----------------------- | ------------------------- |
| 字节数   | 最多 3 字节             | 最多 4 字节               |
| 支持字符 | 基本多语言平面（BMP）   | **完整的 UTF-8**，包括 emoji |
| 存储 emoji | ❌ 报错 `Incorrect string value` | ✅ 正常存储               |
| 推荐     | **不推荐**（阉割版）    | ✅ **生产统一使用**        |

> MySQL 的 `utf8` 是历史上的错误命名——真正的 UTF-8 支持 4 字节，但 MySQL 只实现了 3 字节，导致 emoji（如 😀）和部分生僻汉字（如 𠮷）无法存储。MySQL 8.0 已将默认字符集改为 `utf8mb4`。

---

## 六、锁与并发控制

### 6.1 MySQL 有哪些锁？

**满分答案：**

| 锁类型            | 说明                                                         |
| ----------------- | ------------------------------------------------------------ |
| **全局锁**        | `FLUSH TABLES WITH READ LOCK`，整个库只读，用于全量备份      |
| **表级锁**        | 锁定整张表，MyISAM 使用，InnoDB 在某些情况也会用（如 DDL、手动 LOCK TABLES） |
| **行级锁**        | InnoDB 实现，只锁定需要修改的行，并发高。包括记录锁（Record Lock）、间隙锁（Gap Lock）、临键锁（Next-Key Lock） |
| **共享锁（S锁）** | `SELECT ... LOCK IN SHARE MODE`，允许其他事务读取，但不能修改 |
| **排他锁（X锁）** | `SELECT ... FOR UPDATE`，其他事务不能读也不能写              |
| **意向锁**        | 表级锁，用于表示事务将要给某行加共享锁或排他锁，提高锁检测效率 |

---

### 6.2 什么是锁升级？

**满分答案：**

锁升级是指数据库将**多个细粒度锁（如行锁）合并为一个粗粒度锁（如表锁）**，以节省内存。**InnoDB 不会进行锁升级**（Oracle、SQL Server 会），InnoDB 通过位图等高效方式管理大量行锁。

> 所以面试中如果被问"锁升级"，通常是在考察你对不同数据库区别的了解。

---

### 6.3 说说悲观锁和乐观锁？

**满分答案：**

| 锁类型     | 思想                           | 实现方式                                                     | 适用场景                           |
| ---------- | ------------------------------ | ------------------------------------------------------------ | ---------------------------------- |
| **悲观锁** | 假设每次读写都会冲突，提前加锁 | `SELECT ... FOR UPDATE`，数据库行锁                          | 写多读少、高冲突场景（如库存扣减） |
| **乐观锁** | 假设冲突很少，更新时检查版本   | 使用版本号（version）或时间戳，更新时 `WHERE version = old_version` | 读多写少、低冲突场景（如文章评论） |

**乐观锁示例：**
```sql
-- 查询版本号
SELECT version, balance FROM account WHERE id = 1;  -- version=5
-- 更新时检查版本
UPDATE account SET balance = balance - 100, version = version + 1 
WHERE id = 1 AND version = 5;
-- 如果影响行数为0，说明已被修改，重试或报错
```

---

### 6.4 如何避免死锁？

**满分答案：**

死锁是指两个或多个事务互相持有对方需要的锁，导致无限等待。

**避免死锁的策略：**
1. **按固定顺序访问资源**（如总是先更新 user 表，再更新 order 表）
2. **缩短事务时间**，尽快提交
3. **使用较低的隔离级别**（如 READ COMMITTED 可降低间隙锁）
4. **添加合理索引**，避免全表扫描升级为表锁
5. **使用 `SELECT ... FOR UPDATE` 时注意顺序**
6. **死锁检测**：InnoDB 会自动检测并回滚一个事务（`innodb_deadlock_detect` 参数）

---

### 6.5 记录锁、间隙锁、临键锁详解

InnoDB 行锁的三种具体形式，面试高频追问。

| 锁类型                            | 锁定范围                              | 作用                                                         |
| --------------------------------- | ------------------------------------- | ------------------------------------------------------------ |
| **记录锁（Record Lock）**         | 锁定索引中的某一条**具体记录**        | 等值查询 `WHERE id = 1`，防止其他事务修改/删除该行           |
| **间隙锁（Gap Lock）**            | 锁定索引记录之间的**间隙**（开区间）  | 防止其他事务向间隙中插入新记录，解决**部分幻读**              |
| **临键锁（Next-Key Lock）**       | 记录锁 + 该记录之前的间隙锁（左开右闭） | **InnoDB 默认加锁方式**，同时防止修改/删除和插入              |

**具体示例**——假设表中有 id = 10, 20, 30 三条记录：

```sql
-- 场景1：等值查询 (id 有唯一索引)
SELECT * FROM t WHERE id = 20 FOR UPDATE;
-- 加锁：记录锁，只锁 id=20 这一行

-- 场景2：范围查询
SELECT * FROM t WHERE id > 15 AND id < 25 FOR UPDATE;
-- 加锁：临键锁 (10,20] + 临键锁 (20,30]
-- 效果：id=20 和 id=30 被锁，且 (10,20)、(20,30) 间隙被锁
-- 无法插入 id=16 或 id=21

-- 场景3：等值查询（非唯一索引或数据不存在）
SELECT * FROM t WHERE id = 18 FOR UPDATE;
-- id=18 不存在，落在 (10,20) 间隙中
-- 加锁：间隙锁 (10,20)，阻止插入 id=18
```

> **RR 级别下幻读的克星**：临键锁 = 记录锁 + 间隙锁，既防止已有记录的修改，又防止间隙中插入新记录。这就是 InnoDB 在 RR 级别下避免幻读的核心机制。

---

## 七、分库分表

### 7.1 大表如何优化？

**满分答案：**

| 优化手段         | 说明                                                  |
| ---------------- | ----------------------------------------------------- |
| **查询优化**     | 合理索引、避免全表扫描、使用覆盖索引                  |
| **限定返回数据** | 分页、只查询必要列                                    |
| **读写分离**     | 主库写，从库读，分摊压力                              |
| **垂直分区**     | 将宽表按列拆分成多个表（如将不常用的大字段单独建表）  |
| **水平分区**     | 将同一张表的数据按规则分到不同表（如按 user_id 哈希） |
| **分库分表**     | 数据分散到多个数据库实例                              |
| **数据归档**     | 将历史数据迁移到单独的历史表或备份库                  |

---

### 7.2 分库分表后，主键如何处理？

**满分答案：**

| 方案                      | 说明                                                         | 优点                   | 缺点                             |
| ------------------------- | ------------------------------------------------------------ | ---------------------- | -------------------------------- |
| **UUID**                  | 全局唯一字符串                                               | 简单、无需协调         | 太长、无序（影响 B+ 树插入性能） |
| **数据库自增步长**        | 设置不同数据库的自增步长（如步长=2，实例1生成1,3,5... 实例2生成2,4,6...） | 数值有序               | 扩展不方便，需规划节点数         |
| **雪花算法（Snowflake）** | 64 位 ID（时间戳+机器ID+序列号）                             | 有序、高性能、趋势递增 | 依赖机器时钟                     |
| **Redis 自增**            | 用 Redis `INCR` 生成                                         | 性能好                 | 需要额外组件，有网络开销         |
| **Leaf（美团）**          | 分布式 ID 生成服务                                           | 可靠、高性能           | 需单独部署                       |

> 推荐：雪花算法或其变种（如百度 UIDGenerator、滴滴 TinyID）。

---

## 八、MVCC

### 8.1 什么是 MVCC？

**满分答案：**

MVCC（Multi-Version Concurrency Control，多版本并发控制）是一种**无锁并发控制**方法，通过保存数据的多个版本来实现"读不加锁、读写不冲突"。

**核心原理：**
- 每行记录隐藏两个字段：`DB_TRX_ID`（最后修改该行的事务ID）、`DB_ROLL_PTR`（指向 undo log 中的旧版本链）
- 读操作时，根据事务隔离级别和**可见性规则**（Read View）决定读取哪个版本的数据
- 写操作（UPDATE/DELETE）会生成新版本，并维护旧版本链

**MVCC 解决的问题：**
- 在 `REPEATABLE READ` 和 `READ COMMITTED` 级别下，实现**一致性非锁定读**（Consistent Non-locking Read）
- 避免了脏读、不可重复读（配合间隙锁还可避免幻读）

---

### 8.2 MVCC 是为了解决什么？

**满分答案：**

MVCC 主要解决两个问题：
1. **提高并发性能**：读操作不加锁（除 `FOR UPDATE` 等显式加锁），读写不互斥
2. **实现事务隔离**：让不同事务看到不同版本的数据，满足 `READ COMMITTED` 和 `REPEATABLE READ` 的要求

> 简单记忆：MVCC = 读不加锁 + 多版本 + 可见性规则。

---

## 九、高可用方案

### 9.1 MySQL 高可用方案有哪些？

**满分答案：**

| 方案                                | 原理                                        | 优点                          | 缺点                                      | 适用场景                     |
| ----------------------------------- | ------------------------------------------- | ----------------------------- | ----------------------------------------- | ---------------------------- |
| **主从复制 + 手动切换**             | 一主多从，异步/半同步复制                   | 简单，成本低                  | 故障需人工介入，有延迟                    | 中小型业务，可容忍短暂不可用 |
| **MHA（Master High Availability）** | 自动故障检测和切换，通常配合一主两从        | 切换快（10-30秒），维护成本低 | 管理节点单点（可通过多个MHA Manager解决） | 金融、电商等核心业务         |
| **MGR（MySQL Group Replication）**  | 基于 Paxos 协议的组复制，提供强一致性       | 自动选主，数据一致性高        | 网络延迟敏感，写吞吐较低                  | 强一致性场景                 |
| **InnoDB Cluster**                  | MySQL 官方集成的 MGR + MySQL Shell + Router | 官方支持，运维方便            | 要求 MySQL 5.7+，网络稳定                 | 官方推荐的新方案             |
| **ProxySQL + MGR**                  | 代理层读写分离 + MGR 集群                   | 高可用 + 负载均衡             | 组件多，配置复杂                          | 大型生产环境                 |
| **Orchestrator**                    | 管理 MySQL 拓扑的第三方工具                 | 支持复杂拓扑，可视化          | 学习成本高                                | 多数据中心、大规模集群       |
| **双主 + Keepalived**               | 双主互备，虚拟 IP 漂移                      | 切换快，应用无感知            | 可能产生脑裂，数据冲突                    | 写压力不大的场景             |

**生产推荐：**
- 中小公司：MHA + 主从复制 或 基于 VIP 的双主（需业务去中心化）
- 大公司：InnoDB Cluster 或 MGR + ProxySQL
- 云环境：RDS 自带高可用（如阿里云、AWS RDS Multi-AZ）

---

## 十、Buffer Pool（缓冲池）

### 10.1 什么是 Buffer Pool？

**满分答案：**

Buffer Pool 是 InnoDB 存储引擎的**核心内存结构**，用于缓存数据页和索引页，避免每次读写都访问磁盘。这是 InnoDB 高性能的关键。

| 维度         | 说明                                                         |
| ------------ | ------------------------------------------------------------ |
| **大小**     | 默认 128MB，生产建议设为物理内存的 50%~80%（`innodb_buffer_pool_size`） |
| **存储单位** | 页（Page），默认 16KB                                       |
| **管理算法** | **LRU 变体**（分为 Young 区(5/8) 和 Old 区(3/8)），全表扫描不会污染热点数据 |
| **预读机制** | 线性预读（Linear Read-Ahead）和随机预读（Random Read-Ahead），提前加载可能用到的页 |
| **写回策略** | 脏页通过 Checkpoint 机制异步刷盘，不是每次 UPDATE 都直接写磁盘 |

**常见追问**：
- **Buffer Pool 不够大会怎样？** → 频繁的磁盘 I/O，大量页换入换出，TPS 急剧下降
- **如何查看命中率？** → `SHOW ENGINE INNODB STATUS` 中的 `Buffer pool hit rate`，正常应在 99% 以上

---

## 十一、MySQL 8.0 新特性

面试中能说出新版本特性是很好的加分项。

| 特性                        | 说明                                                         |
| --------------------------- | ------------------------------------------------------------ |
| **原子 DDL**                | DDL 操作（如 ALTER TABLE）现在支持原子性，要么完全成功，要么回滚。解决了 5.7 中 ALTER TABLE 中途失败导致表损坏的问题 |
| **不可见索引**              | `ALTER TABLE t ALTER INDEX idx_name INVISIBLE`，索引不可见后优化器不再使用，用于测试删除索引的影响，安全后再真正删除 |
| **降序索引**                | 支持 `INDEX (a ASC, b DESC)`，对 `ORDER BY a, b DESC` 真正优化，5.7 虽然语法支持但实际忽略 DESC |
| **NOWAIT / SKIP LOCKED**    | `SELECT ... FOR UPDATE NOWAIT`（获取不到锁立即报错而非等待），`SKIP LOCKED`（跳过已锁定的行），适用于秒杀这类高并发排队场景 |
| **窗口函数**                | `ROW_NUMBER()`, `RANK()`, `DENSE_RANK()`, `LAG()`, `LEAD()` 等，媲美 Oracle/PostgreSQL |
| **默认字符集 utf8mb4**      | 彻底告别 utf8 陷阱                                            |
| **双密码**                  | 平滑轮换密码，不停机更新                                     |
| **直方图统计（Histogram）** | `ANALYZE TABLE t UPDATE HISTOGRAM ON col`，对字段的值分布建立统计信息，帮助优化器做更准确的代价估算 |

> **面试加分回答**：提到原子 DDL 和不可见索引，说明你有生产运维经验，不只是背书。

---

## 📋 高频考点速查表（MySQL）

| 分类       | 最高频考点                   | 必会程度 |
| ---------- | ---------------------------- | :------: |
| 存储引擎   | InnoDB vs MyISAM             |  ⭐⭐⭐⭐⭐   |
| 事务       | ACID 特性                    |  ⭐⭐⭐⭐⭐   |
| 事务       | 隔离级别与并发问题           |  ⭐⭐⭐⭐⭐   |
| 事务       | redo log vs binlog + 两阶段提交 |  ⭐⭐⭐⭐⭐   |
| 索引       | B+树原理、聚簇索引、回表     |  ⭐⭐⭐⭐⭐   |
| 索引       | 索引失效场景                 |  ⭐⭐⭐⭐⭐   |
| 索引       | EXPLAIN 执行计划             |  ⭐⭐⭐⭐⭐   |
| 索引       | 索引下推（ICP）              |   ⭐⭐⭐    |
| SQL优化    | 深分页优化                   |   ⭐⭐⭐⭐   |
| 锁机制     | 行锁/间隙锁/临键锁/死锁      |  ⭐⭐⭐⭐⭐   |
| MVCC       | 原理与可见性                 |   ⭐⭐⭐⭐   |
| 分库分表   | 主键生成方案                 |   ⭐⭐⭐    |
| 高可用     | MHA / MGR 区别               |   ⭐⭐⭐    |
| 表设计     | utf8 vs utf8mb4              |   ⭐⭐⭐    |
| 内存       | Buffer Pool 机制             |   ⭐⭐⭐    |
| 版本特性   | MySQL 8.0 新特性              |   ⭐⭐⭐    |
