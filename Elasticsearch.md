# Elasticsearch 面试题

## 一、核心概念与原理

### 1.1 什么是倒排索引？为什么 ES 搜索这么快？

这是 ES 面试的"第一问"。关键词：**倒排索引 + FST + 跳表**。

| 维度                 | 正向索引（MySQL）                       | 倒排索引（Elasticsearch）                       |
| -------------------- | --------------------------------------- | ----------------------------------------------- |
| **映射方向**         | 文档 → 词（ID → 内容）                  | 词 → 文档（Term → DocID 列表）                  |
| **全文检索方式**     | `LIKE '%keyword%'`，全表扫描 O(N)       | 查词典 O(1) → 取倒排链 → 合并，毫秒级          |
| **典型场景**         | `WHERE id = 123`                        | `MATCH content 'elasticsearch 原理'`            |

**倒排索引的三个核心组件**：

| 组件               | 说明                                      | 存储位置        |
| ------------------ | ----------------------------------------- | --------------- |
| **Term Index（词项索引）**   | 词典的"目录"，FST 结构，内存常驻         | `.tip` 文件     |
| **Term Dictionary（词项字典）** | 所有去重词项的有序集合，关联 Posting List 偏移量 | `.tim` 文件     |
| **Posting List（倒排列表）** | 每个词对应的文档 ID 列表 + 词频 + 位置信息    | `.doc` / `.pos` |

**检索过程示例**——搜索 `"elasticsearch 原理"`：
```
① 分词 → ["elasticsearch", "原理"]
② FST 快速定位两个 term → 获取 Posting List 的文件偏移量
③ 读取各自的 Posting List：[doc1, doc3, doc7] 和 [doc1, doc2, doc7]
④ 通过跳表加速合并 → 交集结果 [doc1, doc7]
⑤ 计算 BM25 评分 → 排序返回
```

> **面试官可能会追问**：倒排索引这么好，为什么 MySQL 不默认使用？——倒排索引对写入不友好（每次写入需要分词+更新词典+更新倒排链），MySQL 的 B+ 树是面向 OLTP 的平衡方案，而 ES 是为全文检索场景专门设计的。两者各有所长，不能互相替代。


### 1.2 FST（有限状态转换器）是什么？为什么不用 HashMap？

FST 是 Lucene 用来在内存中存储**词项字典**的核心数据结构。

| 对比     | HashMap                         | FST                                         |
| -------- | ------------------------------- | ------------------------------------------- |
| 内存占用 | 大（存完整 key + value + 指针） | 小（共享前缀和后缀，节省 **50%+** 内存）     |
| 前缀查询 | ❌ 需全量扫描                    | ✅ 天然支持（`prefixQuery` 如 `elastic*`）   |
| 范围查询 | ❌ 不支持                        | ✅ 天然支持（如查找 [abc, abd) 之间的词项）  |
| 模糊查询 | ❌ 不支持                        | ✅ 支持有限编辑距离的模糊匹配                |

**FST 的核心原理**：
- 基于 **确定无环有限状态转换器**，每个节点 + 边构成状态转移图
- 共享前缀（如 `elasticsearch` / `elastic` 共享 `elastic`）和后缀（如 `october` / `november` / `december` 共享 `ember`、`ber`）
- 每条边附带一个 Output 值（如指向 Posting List 的文件偏移量），查询时累加路径上的 Output 即为结果

> **一句话总结**：FST = 共享前后缀的压缩前缀树 + 边权重，是 Lucene 实现内存高效词典的基石。


### 1.3 text 和 keyword 的区别？

| 对比维度     | text                             | keyword                           |
| ------------ | -------------------------------- | --------------------------------- |
| **是否分词** | ✅ 会分词（如"你好世界"→["你好","世界"]） | ❌ 不分词，保持原值           |
| **用途**     | 全文检索（`match` 查询）         | 精确匹配、排序、聚合              |
| **索引结构** | 倒排索引                         | doc_values（列式存储）            |
| **是否支持聚合** | ❌（需开启 fielddata，耗内存） | ✅ 默认支持                        |
| **典型场景** | 文章内容、商品描述、评论         | ID、邮箱、标签、状态码、URL       |
| **最佳实践** | 配置 multi-fields：`"field": {"type": "text", "fields": {"keyword": {"type": "keyword"}}}` | 同左                           |

> `multi-fields` 示例：同一个字段同时建 text + keyword，查询用 `field` 做全文匹配，聚合/排序用 `field.keyword`。


### 1.4 ES 中 segment 是什么？为什么不可变？

| 维度       | 说明                                                         |
| ---------- | ------------------------------------------------------------ |
| **定义**   | Segment 是 Lucene 的**最小索引单元**，每个 Segment 是一个独立的倒排索引 |
| **不可变性** | Segment 一旦写入磁盘就不能被修改 —— 这是 ES 高性能的核心设计 |
| **不可变的好处** | ① 不需要锁（无并发写入冲突）② 常驻 OS Cache （不会因修改而失效）③ 简化缓存和压缩 |
| **更新/删除** | 删除=在 `.del` 文件中标记；更新=标记旧文档删除 + 写入新文档到新 Segment |
| **段合并** | 后台自动将多个小 Segment 合并为大 Segment，物理删除被标记的文档，释放空间 |

> **面试追问**：Segment 多了有什么影响？——每次搜索需要遍历所有 Segment，Segment 越多查询越慢。通过 `_forcemerge` 可强制合并（生产慎用，IO 开销大）。


### 1.5 ES 支持哪些数据类型？

| 类型              | 说明                               | 典型场景                     |
| ----------------- | ---------------------------------- | ---------------------------- |
| **text**          | 全文搜索字段，分词                 | 文章内容、商品描述           |
| **keyword**       | 精确值，不分词                     | ID、标签、邮箱               |
| **数值型**        | `long`, `integer`, `short`, `byte`, `double`, `float` | 价格、年龄、数量       |
| **date**          | 日期类型，支持多种格式             | 订单时间、日志时间戳         |
| **boolean**       | 布尔值                             | 是否删除、是否激活           |
| **geo_point**     | 经纬度坐标                         | 附近门店、LBS 推荐           |
| **dense_vector**  | 密集向量（ES 7.x+）                | 语义搜索、图像检索、推荐     |
| **semantic_text** | 语义文本（ES 9.0 GA），一行配置集成推理模型 | 语义搜索，开箱即用           |
| **nested**        | 嵌套对象（独立索引）               | 数组内对象需要独立查询时     |
| **join**          | 父子文档关联                       | 一对多关系（ES 中尽量避免）  |
| **completion**    | 自动补全（Suggest）                 | 搜索框下拉提示               |


## 二、数据写入与持久化

### 2.1 文档写入的完整流程是什么？

这是 ES 面试的**必问题**，需要讲清楚从客户端到磁盘的完整链路。

```
客户端请求
    │
    ▼
协调节点（Coordinating Node）
    │  shard = hash(_id) % 主分片数
    ▼
主分片节点
    │
    ├──▶ ① Memory Buffer（内存缓冲区）
    │         │
    │         └──▶ ② Translog（事务日志）写入磁盘
    │                    │
    │     ═══════ refresh（默认 1s）═══════
    │                    │
    │                    ▼
    │          ③ Filesystem Cache → 生成新 Segment（此时数据可被搜索！）
    │          清空 Memory Buffer（Translog **不清空**）
    │                    │
    │     ═══════ flush（默认 30min / translog 达 512MB）═══════
    │                    │
    │                    ▼
    │          ④ fsync → 磁盘（持久化） → 写入 Commit Point → 清空 Translog
    │
    ▼
并行同步到副本分片 → 全部成功 → 返回 ACK 给客户端
```

> **面试加分细节**：ES 写入到达主分片后会**并行**发送到所有副本分片，全部返回成功后才向客户端确认。这就是为什么副本数越多写延迟越高。


### 2.2 refresh、flush 和 translog 的区别？

| 对比维度      | refresh                     | flush                        | translog                     |
| ------------- | --------------------------- | ---------------------------- | ---------------------------- |
| **数据流向**  | Memory Buffer → OS Cache    | OS Cache → 磁盘              | 写入时同步记录到磁盘         |
| **频率**      | 默认 1 秒                   | 默认 30 分钟 / 512MB 触发    | 每次请求同步 fsync（ES 6.x+） |
| **代价**      | 轻（内存操作）              | 重（fsync 磁盘 I/O）         | 中等（顺序写）               |
| **目的**      | 数据**可被搜索**（NRT）     | 数据**持久化**到磁盘         | **故障恢复**（WAL）          |
| **是否清空 Buffer** | ✅ 清空 Memory Buffer   | ✅ 清空 Memory Buffer + Translog | —                     |

> **面试官追问**：refresh 设置太频繁会怎样？——CPU 和 I/O 开销增大，写入吞吐量下降。大批量导入时建议设 `-1`（关闭），导入完成再恢复。Translog 太大又会导致节点重启恢复时间变长。


### 2.3 Segment Merge（段合并）机制

| 维度         | 说明                                                         |
| ------------ | ------------------------------------------------------------ |
| **背景**     | 每次 refresh 产生一个新 Segment，Segment 数量持续增长会影响查询效率 |
| **合并策略** | 后台自动将多个小 Segment 合并为大 Segment，物理删除 `.del` 文件标记的文档 |
| **触发条件** | `tiered` 策略（默认），按 Segment 大小分层合并               |
| **性能影响** | 合并过程消耗 CPU 和磁盘 I/O，ES 会自动限制合并线程的 I/O 吞吐 |
| **手动合并** | `POST /index/_forcemerge?max_num_segments=1`（生产慎用，IO 密集） |


## 三、搜索机制

### 3.1 搜索的两阶段过程（Query Then Fetch）

搜索执行分为两个阶段，这是面试的核心追问点：

**Phase 1 — Query 阶段（分散查询）**：

```
协调节点 ──广播──▶ 分片1 ─── 本地查询 ─── 返回 TopN(docId + score)
          ──广播──▶ 分片2 ─── 本地查询 ─── 返回 TopN(docId + score)
          ──广播──▶ 分片3 ─── 本地查询 ─── 返回 TopN(docId + score)
                                            │
                                    协调节点 全局排序 + 取 TopN
```

- 协调节点将查询广播到**所有相关分片**（主分片或副本分片，轮询选择）
- 每个分片在本地执行查询，构建 `from + size` 的优先队列
- 只返回 **(docId, sort_values)** 给协调节点，**不返回文档内容**

**Phase 2 — Fetch 阶段（获取文档）**：

```
协调节点 ── 根据 TopN 的 docId ──▶ 对应分片 ── 拉取完整文档 ──▶ 返回给客户端
```

- 协调节点根据最终排序的 TopN 文档 ID，向对应分片请求完整 `_source`
- 返回完整结果给客户端

> **面试官追问**：为什么分两阶段？——减少网络传输量。Query 阶段只传 docId + score（几百字节），Fetch 阶段才按需拉取完整文档（可能几 KB）。如果 1000 个分片都返回完整文档，协调节点内存直接爆掉。


### 3.2 query 和 filter 的区别？（高频）

| 对比维度   | query（查询上下文）                        | filter（过滤上下文）               |
| ---------- | ------------------------------------------ | ---------------------------------- |
| **评分**   | ✅ 计算相关性得分（`_score`）               | ❌ 不计算，只判断 yes/no           |
| **缓存**   | ❌ 结果不缓存                               | ✅ **自动缓存**（filter cache）     |
| **性能**   | 慢（需计算评分）                           | 快（直接二分查找或位图过滤）       |
| **适用场景** | 全文检索、需要排序的搜索                 | 精确匹配、范围过滤、权限控制       |

> **最佳实践**：全文检索用 `query`（出现在 `bool` 的 `must`/`should` 中），精确条件用 `filter`（出现在 `bool` 的 `filter` 中）。`filter` 的结果会被自动缓存，后续相同条件的查询几乎零开销。


### 3.3 相关性评分：TF-IDF vs BM25

| 对比维度     | TF-IDF                      | BM25（ES 5.0+ 默认）                  |
| ------------ | --------------------------- | ------------------------------------- |
| **词频饱和** | 线性增长（100 次 = 10 次 × 10） | **非线性饱和**（第 1 次出现贡献最大，之后递减） |
| **文档长度** | 未充分考虑                  | 引入 `b` 参数做长度归一化，短文档更占优 |
| **公式**     | `score = TF × IDF`         | `score = IDF × (TF × (k1+1)) / (TF + k1×(1-b+b×L))` |

> **调参建议**：`k1`（词频饱和度，默认 1.2）越大词频影响越大；`b`（长度归一化，默认 0.75）越接近 1 长文档惩罚越大。一般不需要调整，默认值适用大多数场景。


## 四、集群架构

### 4.1 ES 有哪些节点类型？

| 节点类型 | 职责 | 配置 |
|---------|------|------|
| **Master Node** | 集群管理：创建/删除索引、分片分配、节点协调 | `node.master: true` |
| **Data Node** | 数据存储、搜索、聚合 | `node.data: true` |
| **Coordinating Node** | 请求路由、结果合并（默认每个节点都是） | `node.master/node.data: false` |
| **Ingest Node** | 数据预处理（Pipeline），如字段重命名、新增计算字段 | `node.ingest: true` |
| **ML Node**（7.x+） | 机器学习任务（异常检测、推理） | `node.ml: true` |

> **生产推荐**：主节点和数据节点**分离部署**——Master 节点不存数据（避免 GC 影响集群管理），Data 节点专注数据读写。


### 4.2 主分片和副本分片有什么区别？

| 对比维度 | 主分片（Primary） | 副本分片（Replica） |
| -------- | :---------------: | :-----------------: |
| **写操作** | ✅（唯一写入入口） | ❌（写操作必须经过主分片） |
| **读操作** | ✅                 | ✅（负载均衡，分担查询压力） |
| **数量修改** | ❌ 创建时固定，不能改 | ✅ 可随时动态调整 |
| **高可用** | —（丢失=数据不可用） | ✅（主分片丢失时副本可升主） |
| **建议数量** | 根据数据量预估（单分片 10-50GB） | ≥1（生产至少 1 个副本） |

> **路由公式**：`shard = hash(_routing) % number_of_primary_shards`，默认 `_routing` = `_id`。主分片数一旦确定就无法修改，因为路由算法依赖它——修改需通过 Reindex 重建索引。


### 4.3 Master 选举和脑裂问题

| 维度 | 说明 |
|------|------|
| **选举算法** | ES 7.x+ 使用基于 Raft 变种的共识算法，通过 `cluster.initial_master_nodes` 配置候选节点 |
| **选举条件** | 候选节点需获得超过半数投票（`N/2 + 1` 多数派） |
| **脑裂原因** | 网络分区、节点 GC 停顿过长、Master 假死 |
| **脑裂后果** | 出现多个 Master，各自独立分配分片，数据写入冲突严重 |

**预防措施**：
- ES 6.x：配置 `discovery.zen.minimum_master_nodes = N/2 + 1`
- ES 7.x+：使用 `cluster.initial_master_nodes` 配置启动种子节点列表，内置多数派仲裁
- 建议 Master 候选节点数为**奇数**（3 / 5 / 7），避免平票


### 4.4 集群健康状态

| 状态 | 含义 | 影响 | 处理 |
|:---:|------|------|------|
| **🟢 Green** | 所有主分片和副本分片均已分配 | 正常 | — |
| **🟡 Yellow** | 所有主分片正常，但部分副本分片未分配 | 数据安全，但缺少冗余（单节点 ES 必然 Yellow） | 增加节点 / 增加副本 |
| **🔴 Red** | 至少一个主分片未分配（丢失） | **数据不可用**，对应索引操作均失败 | 紧急恢复：检查磁盘空间、节点状态、恢复 snapshot |

> `GET _cluster/health` 查看集群健康状态。Yellow 在生产中常见（单节点），Red 必须立刻处理。


## 五、查询 DSL 与性能优化

### 5.1 深度分页问题及解决方案

| 方案 | 原理 | 适用场景 | 优缺点 |
|------|------|---------|--------|
| **from + size** | 直接跳过前 N 条取后 M 条 | 浅分页（前 100 页） | ⚠️ `from + size > 10000` 会触发 `index.max_result_window` 限制 |
| **search_after** ✅ | 基于上一页最后一条的 sort 值，跳过前面的数据 | **深度翻页（实时）** | ✅ 性能稳定，不支持跳页（只能上一页/下一页） |
| **scroll** | 生成快照游标，后续翻页基于该快照 | 全量导出 / 数据迁移 | ✅ 性能好，但非实时（快照后数据变更不可见），需手动清理 |
| **PIT（Point In Time）** | ES 7.10+，比 scroll 更轻量的快照 | 取代 scroll 的新方案 | ✅ 轻量快照 + search_after，推荐的深度分页方案 |

**search_after 示例**：
```json
// 第一页
GET /index/_search
{"size": 20, "sort": [{"id": "asc"}], "query": {...}}

// 第二页（传入上一页最后一条的 sort 值）
GET /index/_search
{"size": 20, "sort": [{"id": "asc"}], "search_after": [1000019], "query": {...}}
```


### 5.2 写入性能优化

| 优化方向 | 具体措施 | 效果 |
|---------|---------|------|
| **批量写入** | 使用 Bulk API，每批 5-15MB | 减少网络往返，吞吐量提升 10x+ |
| **关闭 refresh** | `refresh_interval=-1`（导入完成恢复） | 减少 segment 生成开销 |
| **关闭副本** | `number_of_replicas=0`（导入后恢复） | 避免副本同步开销 |
| **自动生成 ID** | 让 ES 自动生成 `_id` | 避免 `_id` 唯一性检查 |
| **异步 Translog** | `index.translog.durability: async` | 减少 fsync 开销（略有丢数据风险） |
| **增加 Bulk 线程池** | `thread_pool.write.queue_size` | 提升写入并发度 |


### 5.3 查询性能优化

| 优化方向 | 具体措施 |
|---------|---------|
| **用 filter 替代 query** | `bool.filter` 不计算评分 + 结果可缓存，性能远优于 `bool.must` |
| **keyword + doc_values** | 聚合和排序字段用 `keyword`，底层走列式存储而非倒排索引 |
| **避免通配符开头** | `*abc` 会触发全量词项扫描，应尽量避免 |
| **限制查询范围** | 先用时间范围缩小查询域（如只查近 7 天） |
| **使用 routing** | 查询时指定 `_routing`，只访问特定分片，减少不必要的数据扫描 |
| **索引 sorting** | 配置 `index.sort.field`，数据写入时预排序，查询时跳过不必读的 segment |
| **Mapping 优化** | 关闭不需要的 `_source`/`doc_values`/`norms`；设置 `dynamic: strict` 防字段爆炸 |


### 5.4 集群与系统级调优

| 优化项 | 推荐配置 | 原因 |
|--------|---------|------|
| **JVM 堆内存** | 物理内存 50%，但**不超过 32GB** | 超过 32GB 失去压缩普通对象指针，内存利用率反而下降 |
| **禁止 Swap** | `bootstrap.memory_lock: true` | 内存换出到磁盘会导致灾难性性能抖动 |
| **文件描述符** | `ulimit -n 65536` | ES 使用大量文件（每个 segment 多文件），默认 1024 远不够 |
| **磁盘** | SSD 优先，RAID 0/10 | 随机读写密集型，SSD 是标配 |
| **单分片大小** | 10-50GB（不超过 100GB） | 太大：迁移/恢复慢；太小：分片管理开销大 |


## 六、数据同步与 ELK 生态

### 6.1 MySQL 数据如何同步到 ES？

| 方案 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| **Binlog 同步（Canal/Debezium）** ✅ | 伪装 MySQL Slave，监听 Binlog 变更推送到 ES | 解耦、可靠性高、无业务侵入 | 架构复杂，有同步延迟 |
| **MQ 异步通知** | 业务代码发 MQ → 消费端写 ES | 低耦合、易扩展 | 依赖 MQ 可靠性，数据可能不一致 |
| **同步双写** | 业务代码同时写 MySQL + ES | 实时性最好 | 强耦合、双写失败难处理 |
| **定时全量/增量同步** | `SELECT * WHERE update_time > last_sync_time` | 简单 | 延迟大、无法感知 DELETE |

> **生产推荐**：Canal + MQ + ES（阿里、美团等大厂主流方案）。Canal 监听 Binlog → 推送到 Kafka/RocketMQ → 消费端写 ES，峰值 QPS 可达数万。


### 6.2 ELK 技术栈

| 组件 | 职责 | 替代/增强 |
|------|------|----------|
| **E**lasticsearch | 存储、搜索、分析 | — |
| **L**ogstash | 数据采集和处理管道 | Filebeat（更轻量的采集器） |
| **K**ibana | 可视化、仪表盘、Dev Tools | Grafana（监控可视化） |

**现代日志架构**：
```
应用服务器 ── Filebeat ──▶ Kafka ──▶ Logstash ──▶ Elasticsearch ──▶ Kibana
              (轻量采集)     (削峰)    (加工/清洗)    (存储检索)     (可视化)
```

> 引入 Kafka 的作用：**削峰填谷**。日志量突增时 Kafka 缓存，防止 ES 写入被打爆。


## 七、索引生命周期管理（ILM）与冷热分离

### 7.1 冷热分离架构

| 层级 | 存储介质 | 数据特征 | 分片分配 |
|------|---------|---------|---------|
| **Hot（热）** | SSD，高配节点 | 最近写入/频繁查询（如近 3 天） | `"data": "hot"` |
| **Warm（温）** | SSD/高速 HDD | 偶尔查询（如近 30 天） | `"data": "warm"` |
| **Cold（冷）** | HDD，低配节点 | 极少查询（如近 1 年） | `"data": "cold"` |
| **Delete** | — | 超出保留期 | 自动删除 |

### 7.2 ILM（Index Lifecycle Management）

自动管理索引从创建到删除的全生命周期：

```
创建索引 ── rollover（达到指定大小/条数/时间）──▶ 新索引
    │
    ▼
Hot Phase ──▶ Warm Phase ──▶ Cold Phase ──▶ Delete Phase
(写优化)       (只读+合并)     (冻结)         (删除)
```

**配置示例要点**：
- `rollover` 条件：`max_size: 50GB` 或 `max_age: 1d` 或 `max_docs: 10000000`
- Hot → Warm：索引创建后 3 天，执行 `forcemerge` + `shrink`
- Warm → Cold：索引创建后 30 天，`freeze` 冻结（内存换出）
- Delete：索引创建后 90 天

> 冷热分离可降低约 **60%** 的存储成本，是大数据量日志/指标场景的标准方案。


## 八、ES 8.x / 9.x 新特性与趋势

面试中能说出新版本特性是很大的加分项。目前（2026年）最新版本为 **ES 9.2**，8.x 将于 2027年1月停止维护。

### 8.1 ES 8.x 核心新特性

| 特性 | 版本 | 说明 |
|------|------|------|
| **Security 默认开启** | 8.0+ | SSL/TLS 默认强制，告别"裸奔"模式 |
| **ES\|QL** | 8.11+ | 全新的管道式查询语言，类 SQL 语法，支持 Join、聚合、时间序列 |
| **PIT（Point In Time）** | 7.10+ | 轻量级快照替代 scroll，配合 search_after 高效深度分页 |
| **Synthetic _source** | 8.x | `_source` 不存储，读取时从 doc_values 重建，节省 ~40% 磁盘 |
| **KNN 向量搜索** | 7.x+ / 8.x 增强 | `dense_vector` + `knn` 查询，语义搜索和以图搜图 |
| **混合搜索** | 8.x | `knn` + `query` 组合查询，关键词 + 语义双通道召回 |
| **禅定搜索(Binary Quantization)** | 8.x 预览 | 1-bit 向量量化，内存暴降 |

### 8.2 ES 9.x 重磅更新（2025-2026）

**ES 9.0（2025年4月）**：

| 特性 | 说明 |
|------|------|
| **Lucene 10 底层升级** | 更好的并行性、更智能的索引策略、硬件感知优化。数值聚合性能**翻倍**（SIMD 加速 doc_values） |
| **semantic_text 字段 GA** | 将推理模型直接集成到 Mapping 中，一行配置搞定语义搜索，告别繁琐的 Pipeline 设置 |
| **BBQ（Better Binary Quantization）** | int8/int4 标量量化 GA，索引体积分别缩小 **4×/8×**，召回率保持 95%+ |
| **DiskBBQ** | 向量直接从磁盘读取量化数据，**<20ms 延迟仅需 100MB 内存**，告别全量向量加载 |
| **rank_vectors 字段（实验性）** | ColBERT 风格的多向量延迟交互排序，精细化相关性 |
| **ES\|QL LOOKUP JOIN** | 跨索引/跨集群查表关联，支持 `=`、`<`、`>`、`!=` 多字段条件 |
| **Entitlements 安全模型** | 替代 Java SecurityManager，更细粒度的运行时权限控制 |
| **Java 21+** | 强制要求 JDK 21，利用虚拟线程等新特性 |
| **Transport Protocol v9** | 节点间通信协议升级，旧版客户端不兼容 |

**ES 9.2（2026年初，当前最新）**：

| 特性 | 说明 |
|------|------|
| **ES\|QL 混合搜索** | `FORK` + `FUSE` 命令实现多路检索 → RRF/线性融合 → 语义重排，ES\|QL 一条命令完成全流程 |
| **磁盘向量搜索(GA)** | DiskBBQ 增强版，无需预热即可低延迟检索 |
| **Streams（可观测性）** | AI 自动解析原始日志、提取字段、标记"Significant Events"，直指根因 |
| **Agent Builder** | AI 驱动的 Agent 构建器，与 ES 数据原生对话 |
| **Background Search** | 异步长查询（跨年数据不阻塞），Discover 中直接提交后台任务 |
| **Dashboard 自动迁移** | Splunk → Elastic 仪表板一键迁移（技术预览） |
| **ES\|QL 时间序列命令** | `RATE`、`*_OVER_TIME`、`TBUCKET`、`TS` 原生集成到 Discover |

### 8.3 未来路线图（2026+）

| 方向 | 内容 |
|------|------|
| **稀疏向量查询** | ES\|QL 原生支持稀疏向量检索 |
| **语义重排长文档** | 长文本场景的语义 Reranking 增强 |
| **多 FORK 分支** | ES\|QL 条件并行执行多个查询分支 |
| **全文搜索运行时列** | 动态生成字段的全文检索 |
| **进一步 BBQ 优化** | 更多向量压缩策略 |

> **面试加分回答**：提到 ES 9.x 的 Lucene 10、semantic_text、BBQ/DiskBBQ 和 ES\|QL 混合搜索，说明你对 ES 技术前沿有持续关注。ES 9.x 的核心趋势是——**向量搜索平民化**（一行配置开箱即用）+ **ES\|QL 大一统**（搜索→聚合→Join→混合检索→Rerank 全部管道化）。


## 📋 高频考点速查表（Elasticsearch）

| 分类 | 最高频考点 | 必会程度 |
|------|-----------|:---:|
| 基础 | 倒排索引原理（FST + Posting List） | ⭐⭐⭐⭐⭐ |
| 基础 | text vs keyword + multi-fields | ⭐⭐⭐⭐⭐ |
| 写入 | 写入流程（refresh / flush / translog） | ⭐⭐⭐⭐⭐ |
| 搜索 | Query Then Fetch 两阶段 | ⭐⭐⭐⭐⭐ |
| 搜索 | query vs filter 区别 | ⭐⭐⭐⭐⭐ |
| 搜索 | 深度分页（search_after / scroll / PIT） | ⭐⭐⭐⭐ |
| 集群 | 节点类型 + 主/副分片区别 | ⭐⭐⭐⭐⭐ |
| 集群 | Master 选举 + 脑裂 + 健康状态 | ⭐⭐⭐⭐ |
| 性能 | 写入优化（Bulk / refresh_interval / 副本） | ⭐⭐⭐⭐ |
| 性能 | 查询优化（filter / routing / 冷热分离） | ⭐⭐⭐⭐ |
| 数据同步 | MySQL → ES（Canal / MQ / 双写） | ⭐⭐⭐⭐ |
| 底层 | Segment 不可变性 + Merge | ⭐⭐⭐ |
| 底层 | BM25 评分原理 | ⭐⭐⭐ |
| 生态 | ELK 架构 + Kafka 削峰 | ⭐⭐⭐ |
| 趋势 | 向量搜索 / ES\|QL / 9.x 新特性 | ⭐⭐⭐ |
